# Flashcard Generator (Русский)

**Flashcard Generator** — это удобный инструмент для автоматической генерации карточек «вопрос-ответ» из текстов на русском языке. Проект подходит для обучения, подготовки к экзаменам, создания шпаргалок и повторения материала.

С помощью этой программы вы можете:
- Генерировать карточки из текстов `.txt`, `.md` или `.pdf`.
- Использовать эвристический метод или трансформерную модель для создания вопросов.
- Экспортировать результаты в JSON, CSV, Markdown или PDF.
- Запускать веб-интерфейс через Streamlit для удобной работы в браузере.

---

## ⚡ Быстрый старт

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/3Zenyl3/Flashcard-Generator.git
cd flashcard-generator
```

> 💡 Замените URL на актуальный адрес вашего репозитория.

### 2. Установите зависимости

Все необходимые библиотеки перечислены в `requirements.txt`. Установите их:

```bash
pip install -r requirements.txt
```

> ⚠️ Для использования режима `transformer` дополнительно потребуются `torch` и `transformers`. Они включены в `requirements.txt`, но при необходимости можно установить вручную.

### 3. Подготовьте структуру проекта

```bash
mkdir -p input output data
```

- `input/` — сюда кладите исходные тексты (`.txt`, `.md`, `.pdf`).
- `output/` — сюда будут сохраняться сгенерированные карточки.
- `data/` — резервная папка для будущих расширений (например, кэширование моделей).

### 4. Генерация карточек

Используйте команду вида:

```bash
python src/flashcard_generator.py --input input/<имя_файла> --output output/<имя_файла>.json --mode heuristic
```

**Пример** (с тестовым текстом о Франции):

```bash
python src/flashcard_generator.py --input input/sample_france.txt --output output/sample_france.json --mode heuristic
```

#### Параметры:
- `--mode heuristic` — использует правила на основе синтаксиса и ключевых слов (быстро, не требует GPU).
- `--mode transformer` — использует предобученную модель `cointegrated/rut5-base-question-generator` для генерации (качественнее, но медленнее и требует больше памяти).

### 5. Использование Streamlit UI (опционально)

Если установлен **Streamlit**, можно запустить веб-интерфейс:

```bash
python src/flashcard_generator.py --serve
```

Откроется локальный сервер (обычно `http://localhost:8501`). Через него можно:
- Загружать файлы в форматах `.txt`, `.md`, `.pdf`.
- Выбирать режим генерации.
- Просматривать карточки прямо в браузере.

---

## 📂 Структура проекта

```bash
flashcard-generator/
├─ src/flashcard_generator.py # Основной односегментный скрипт
├─ input/ # Входные тексты для обработки
├─ output/ # Сгенерированные карточки
├─ requirements.txt # Зависимости
├─ README.md # Эта документация
└─ tests/ # Тестовые файлы и примеры
```

---

## 📦 Требуемые библиотеки

| Библиотека | Назначение |
|-----------------|------------|
| `razdel` | Токенизация и разбиение на предложения (для русского языка) |
| `pymorphy2` | Лемматизация и морфологический анализ |
| `scikit-learn` + `numpy` | TF-IDF, косинусное сходство, ранжирование предложений |
| `pdfplumber` | Извлечение текста из PDF |
| `streamlit` | Веб-интерфейс (опционально) |
| `transformers` + `torch` | Генерация вопросов с помощью RuT5 (опционально) |
| `reportlab` | Экспорт карточек в PDF |

> Все зависимости устанавливаются одной командой: `pip install -r requirements.txt`.

---

## 🚀 Пример работы

После выполнения:

```bash
python src/flashcard_generator.py --input input/sample_france.txt --output output/sample_france.json --mode heuristic
```

Вы получите файл `output/sample_france.json` со следующим содержимым:

```json
[
{
"question": "Что такое Франция?",
"answer": "Франция — это государство в Западной Европе с богатой историей и культурным наследием."
},
{
"question": "Когда: В 1789 году произошла Великая французская революция... ?",
"answer": "В 1789 году произошла Великая французская революция, которая заложила основы современной демократической республики."
}
]
```

---

## 📖 Лицензия

Этот проект распространяется под лицензией **MIT License** — вы можете свободно использовать, изменять и распространять его как в личных, так и в коммерческих целях.

---

> 💬 **Совет**: Для лучшего качества карточек используйте структурированные тексты (определения, даты, факты). Эвристика особенно хорошо работает с конструкциями вида «X — это …» или «В [год] произошло …».

Удачной учёбы! 📚✨
